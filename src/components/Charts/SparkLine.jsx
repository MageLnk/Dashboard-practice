import {
  SparklineComponent,
  Inject,
  SparklineTooltip,
} from "@syncfusion/ej2-react-charts";
// App
const SparkLine = ({ id, data, type, color, width, height, currentColor }) => {
  return <div>SparkLine</div>;
};

export default SparkLine;
